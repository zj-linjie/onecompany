# OneCompany Multi-Agent 框架使用指南

## 快速开始

### 1. 启动 CLI

```bash
npm run start
```

### 2. 选择 Agent 协作模式

在主菜单中选择 `4. Agent 协作模式 🤖`

## 使用方式

### 方式一：直接输入需求

适合简单、明确的需求。

**示例：**
```
需求描述: 实现用户登录功能，包括邮箱密码登录和 JWT 认证
```

系统会自动：
1. 分解为多个子任务（API 设计、后端实现、前端表单、测试等）
2. 分析任务依赖关系
3. 并行执行独立任务
4. 自动审查质量

### 方式二：从需求文档读取

适合复杂、详细的需求文档。

**步骤：**
1. 准备需求文档（Markdown、文本等格式）
2. 在 CLI 中选择 `2. 从文件读取需求文档`
3. 输入文档路径（支持相对路径和绝对路径）

**示例文档路径：**
- 相对路径：`docs/requirements.md`
- 绝对路径：`/Users/apple/Documents/project-spec.md`

## 需求文档模板

### 基础模板

```markdown
# 项目需求文档

## 项目概述
简要描述项目目标和背景

## 功能需求

### 1. 用户认证
- 用户注册（邮箱 + 密码）
- 用户登录（JWT 认证）
- 密码重置功能

### 2. 用户管理
- 查看用户资料
- 编辑用户信息
- 上传头像

### 3. 权限控制
- 基于角色的访问控制（RBAC）
- 管理员、普通用户角色

## 技术要求
- 后端：Node.js + Express
- 前端：React + TypeScript
- 数据库：PostgreSQL
- 认证：JWT

## 质量要求
- 单元测试覆盖率 > 80%
- API 响应时间 < 200ms
- 安全：防止 SQL 注入、XSS 攻击
```

### 详细模板

```markdown
# [项目名称] 需求规格说明书

## 1. 项目背景
[描述项目的业务背景和目标]

## 2. 用户故事

### 2.1 用户注册
**作为** 新用户
**我想要** 使用邮箱注册账号
**以便** 使用系统功能

**验收标准：**
- [ ] 邮箱格式验证
- [ ] 密码强度检查（至少 8 位，包含字母和数字）
- [ ] 发送验证邮件
- [ ] 注册成功后自动登录

### 2.2 用户登录
**作为** 已注册用户
**我想要** 使用邮箱和密码登录
**以便** 访问我的账户

**验收标准：**
- [ ] 支持邮箱 + 密码登录
- [ ] 登录失败 3 次后锁定账户 15 分钟
- [ ] 返回 JWT token
- [ ] Token 有效期 7 天

## 3. 功能模块

### 3.1 认证模块
- POST /api/auth/register - 用户注册
- POST /api/auth/login - 用户登录
- POST /api/auth/logout - 用户登出
- POST /api/auth/refresh - 刷新 token
- POST /api/auth/reset-password - 重置密码

### 3.2 用户模块
- GET /api/users/me - 获取当前用户信息
- PUT /api/users/me - 更新用户信息
- POST /api/users/me/avatar - 上传头像
- DELETE /api/users/me - 删除账户

## 4. 数据模型

### User 表
```sql
CREATE TABLE users (
  id UUID PRIMARY KEY,
  email VARCHAR(255) UNIQUE NOT NULL,
  password_hash VARCHAR(255) NOT NULL,
  name VARCHAR(100),
  avatar_url VARCHAR(500),
  role VARCHAR(20) DEFAULT 'user',
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
```

## 5. 非功能需求

### 5.1 性能
- API 响应时间 < 200ms (P95)
- 支持 1000 并发用户

### 5.2 安全
- 密码使用 bcrypt 加密（salt rounds = 10）
- JWT 使用 RS256 算法
- 所有 API 使用 HTTPS
- 实施 CORS 策略
- 防止 SQL 注入、XSS、CSRF 攻击

### 5.3 可用性
- 系统可用性 > 99.9%
- 数据库每日备份

### 5.4 测试
- 单元测试覆盖率 > 80%
- 集成测试覆盖所有 API 端点
- E2E 测试覆盖核心用户流程

## 6. 技术栈
- **后端**: Node.js 18+, Express 4.x, TypeScript 5.x
- **数据库**: PostgreSQL 15+
- **ORM**: Prisma 5.x
- **认证**: JWT (jsonwebtoken)
- **测试**: Vitest, Supertest
- **前端**: React 18+, TypeScript, Vite
- **状态管理**: Zustand
- **UI 组件**: shadcn/ui

## 7. 开发规范
- 使用 ESLint + Prettier
- Git commit 遵循 Conventional Commits
- 代码审查必须通过才能合并
- 所有 API 必须有 OpenAPI 文档

## 8. 里程碑

### Phase 1: 基础认证 (Week 1-2)
- 用户注册、登录、登出
- JWT 认证中间件
- 基础测试

### Phase 2: 用户管理 (Week 3)
- 用户资料 CRUD
- 头像上传
- 密码重置

### Phase 3: 权限控制 (Week 4)
- RBAC 实现
- 管理员功能
- 完整测试覆盖
```

## 工作流程示例

### 场景：实现用户认证系统

1. **准备需求文档**
   ```bash
   # 创建需求文档
   cat > /tmp/auth-requirements.md << 'EOF'
   # 用户认证系统需求

   ## 功能需求
   1. 用户注册（邮箱 + 密码）
   2. 用户登录（JWT 认证）
   3. Token 刷新机制
   4. 密码重置功能

   ## 技术要求
   - 使用 bcrypt 加密密码
   - JWT token 有效期 7 天
   - 实现 refresh token 机制
   - 单元测试覆盖率 > 80%
   EOF
   ```

2. **启动 OneCompany**
   ```bash
   npm run start
   ```

3. **选择 Agent 协作模式**
   - 选择 `4. Agent 协作模式 🤖`
   - 选择项目（或先创建新项目）

4. **提供需求文档**
   - 选择 `2. 从文件读取需求文档`
   - 输入路径：`/tmp/auth-requirements.md`

5. **查看任务分解**
   系统会自动分解为：
   ```
   1. [architecture] 设计认证系统架构和数据模型
   2. [backend] 实现用户注册 API
   3. [backend] 实现用户登录和 JWT 生成
   4. [backend] 实现 Token 刷新机制
   5. [backend] 实现密码重置功能
   6. [testing] 编写认证系统集成测试
   ```

6. **确认执行**
   - 输入 `y` 开始执行
   - 系统会并行执行独立任务
   - 自动保存进度到 `.onecompany/`

7. **查看结果**
   - 查看任务完成状态
   - 检查生成的代码和测试
   - 查看执行日志

## 配置选项

在 `runAgentMode()` 中可以调整配置：

```typescript
const orchestrator = new TaskOrchestrator({
  maxParallelTasks: 2,        // 最大并行任务数（建议 2-3）
  enableReview: false,        // 是否启用两阶段审查
  enablePersistence: true,    // 是否持久化状态
});
```

### 配置说明

- **maxParallelTasks**: 控制同时执行的任务数
  - `1`: 串行执行（最安全，但较慢）
  - `2-3`: 平衡模式（推荐）
  - `4+`: 高并发（需要更多 API 配额）

- **enableReview**: 两阶段审查
  - `true`: 启用规格审查 + 代码审查（更严格）
  - `false`: 跳过审查（更快，适合原型开发）

- **enablePersistence**: 状态持久化
  - `true`: 保存任务状态，支持恢复（推荐）
  - `false`: 不保存状态（仅用于测试）

## 状态文件

执行后会在项目目录下生成：

```
.onecompany/
├── tasks.json           # 任务队列状态
├── execution-log.json   # 执行日志
└── agents.json          # Agent 配置（未来）
```

### tasks.json 示例

```json
[
  {
    "id": "task-123",
    "title": "实现用户注册 API",
    "description": "...",
    "type": "backend",
    "status": "completed",
    "dependencies": ["task-122"],
    "priority": 1,
    "createdAt": "2024-01-01T00:00:00.000Z",
    "startedAt": "2024-01-01T00:01:00.000Z",
    "completedAt": "2024-01-01T00:05:00.000Z",
    "result": {
      "success": true,
      "output": "..."
    }
  }
]
```

## 最佳实践

### 1. 需求文档编写
- ✅ 使用清晰的标题和章节
- ✅ 包含验收标准
- ✅ 指定技术栈和约束
- ✅ 提供数据模型示例
- ❌ 避免模糊的描述
- ❌ 不要遗漏非功能需求

### 2. 任务粒度
- ✅ 每个任务 2-5 分钟可完成
- ✅ 任务之间依赖关系清晰
- ❌ 避免过大的任务（> 10 分钟）
- ❌ 避免过细的任务（< 1 分钟）

### 3. 并行执行
- ✅ 独立任务可以并行
- ✅ 前端和后端可以同时开发
- ❌ 有依赖的任务必须串行
- ❌ 不要设置过高的并行数

### 4. 审查机制
- ✅ 重要功能启用审查
- ✅ 生产代码启用审查
- ❌ 原型开发可以跳过
- ❌ 简单任务可以跳过

## 故障排查

### 问题：任务分解失败
**原因**: 需求描述不清晰或过于复杂
**解决**:
- 简化需求描述
- 分多次执行
- 提供更详细的技术要求

### 问题：任务执行失败
**原因**: 依赖未满足或环境问题
**解决**:
- 检查 `.onecompany/execution-log.json`
- 查看具体错误信息
- 手动修复后重新执行

### 问题：并行任务冲突
**原因**: 多个任务修改同一文件
**解决**:
- 降低 `maxParallelTasks`
- 调整任务依赖关系
- 使用串行模式

## 进阶用法

### 恢复中断的任务

```bash
# 任务状态已保存在 .onecompany/tasks.json
# 重新启动 CLI 后可以继续执行未完成的任务
npm run start
# 选择 Agent 协作模式
# 系统会自动检测未完成的任务
```

### 自定义 Agent Prompt

编辑 `packages/core/src/prompts/` 下的文件：
- `pm-agent-prompt.md` - 产品经理 Agent
- `dev-agent-prompt.md` - 开发者 Agent
- `spec-reviewer-prompt.md` - 规格审查 Agent
- `code-reviewer-prompt.md` - 代码审查 Agent

### 查看执行日志

```bash
# 查看任务状态
cat workspaces/your-project/.onecompany/tasks.json | jq

# 查看执行日志
cat workspaces/your-project/.onecompany/execution-log.json | jq

# 实时监控（如果任务正在执行）
tail -f workspaces/your-project/.onecompany/execution-log.json
```

## 示例项目

查看 `examples/` 目录下的示例项目：
- `examples/simple-auth/` - 简单认证系统
- `examples/todo-app/` - Todo 应用
- `examples/blog-platform/` - 博客平台

每个示例都包含：
- `requirements.md` - 需求文档
- `expected-tasks.json` - 预期的任务分解
- `README.md` - 使用说明

## 常见问题

**Q: 可以中途修改需求吗？**
A: 可以。重新运行 Agent 模式，系统会基于当前代码状态继续工作。

**Q: 如何控制成本？**
A:
- 降低 `maxParallelTasks`
- 禁用 `enableReview`
- 将大需求拆分为多次执行

**Q: 支持哪些项目类型？**
A: 目前支持 Web 应用（前端 + 后端）。未来会支持移动应用、CLI 工具等。

**Q: 生成的代码质量如何？**
A:
- 启用审查模式可以提高质量
- 建议人工审查关键代码
- 可以通过自定义 prompt 提高质量

## 反馈与贡献

- 问题反馈：[GitHub Issues](https://github.com/yourusername/onecompany/issues)
- 功能建议：[GitHub Discussions](https://github.com/yourusername/onecompany/discussions)
- 贡献代码：查看 `CONTRIBUTING.md`
