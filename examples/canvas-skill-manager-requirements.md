# Canvas Skill & Agent 管理系统需求文档

## 1. 项目概述

构建一个可视化的 Canvas 应用，允许用户通过拖拽方式管理项目的 Skills 和 Agents，实现直观的配置和专家系统加载。

## 2. 核心功能

### 2.1 Canvas 画布系统

**功能描述**：
- 提供一个无限画布（Infinite Canvas）
- 支持缩放（Zoom）和平移（Pan）
- 网格背景辅助对齐
- 支持多选和框选

**技术要求**：
- 使用 React Flow 或 Konva.js 实现
- 支持触摸屏和鼠标操作
- 流畅的 60fps 渲染

### 2.2 Skill 节点系统

**功能描述**：
- 从 Skills Catalog 中拖拽 Skill 到画布
- 每个 Skill 显示为一个卡片节点
- 节点显示：图标、名称、描述、状态（启用/禁用）
- 支持连接线连接到项目节点

**Skill 节点属性**：
```typescript
interface SkillNode {
  id: string;
  type: 'skill';
  skillId: string;        // 对应 skills-catalog 中的 skill
  name: string;
  description: string;
  enabled: boolean;       // 开关状态
  position: { x: number; y: number };
  connections: string[];  // 连接到的项目/Agent ID
}
```

**交互功能**：
- 点击节点切换启用/禁用状态
- 拖拽节点移动位置
- 双击编辑配置
- 右键菜单：删除、复制、查看详情

### 2.3 Agent 节点系统

**功能描述**：
- 从 Agent 库中拖拽 Agent 到画布
- Agent 节点显示：角色、技能列表、状态
- 可以连接多个 Skills 到 Agent
- Agent 作为"专家"加载到项目

**Agent 节点属性**：
```typescript
interface AgentNode {
  id: string;
  type: 'agent';
  role: AgentRole;        // 'backend-dev', 'frontend-dev', etc.
  name: string;
  skills: string[];       // 关联的 skill IDs
  enabled: boolean;
  position: { x: number; y: number };
  specialization: string; // 专家领域描述
}
```

**Agent 类型**：
- Backend Developer (后端开发专家)
- Frontend Developer (前端开发专家)
- DevOps Engineer (运维专家)
- Security Expert (安全专家)
- Performance Optimizer (性能优化专家)
- Testing Specialist (测试专家)

### 2.4 项目节点

**功能描述**：
- 中心节点代表当前项目
- 显示项目名称、类型、状态
- 所有 Skills 和 Agents 连接到项目节点

**项目节点属性**：
```typescript
interface ProjectNode {
  id: string;
  type: 'project';
  name: string;
  path: string;
  activeSkills: string[];   // 启用的 skills
  activeAgents: string[];   // 加载的 agents
  position: { x: number; y: number };
}
```

### 2.5 连接线系统

**功能描述**：
- 可视化显示 Skill/Agent 与项目的关联
- 不同类型的连接使用不同颜色
- 连接线显示数据流向

**连接类型**：
```typescript
interface Connection {
  id: string;
  source: string;         // 源节点 ID
  target: string;         // 目标节点 ID
  type: 'skill-to-project' | 'agent-to-project' | 'skill-to-agent';
  enabled: boolean;       // 连接是否激活
  style: {
    color: string;
    width: number;
    animated: boolean;    // 激活时动画效果
  };
}
```

**连接规则**：
- Skill → Project：直接启用 skill
- Agent → Project：加载 agent 到项目
- Skill → Agent：为 agent 添加特定 skill
- 禁止循环连接

### 2.6 侧边栏 - Skills Catalog

**功能描述**：
- 左侧面板显示所有可用 Skills
- 分类展示（按角色或功能分组）
- 搜索和过滤功能
- 拖拽到画布创建节点

**UI 布局**：
```
┌─────────────────────────────────────────┐
│ Skills Catalog                    [搜索] │
├─────────────────────────────────────────┤
│ 📦 Backend Skills                       │
│   ├─ 🔧 API Development                 │
│   ├─ 🗄️  Database Design                │
│   └─ 🔐 Authentication                  │
│                                         │
│ 🎨 Frontend Skills                      │
│   ├─ ⚛️  React Development              │
│   ├─ 🎭 UI/UX Design                    │
│   └─ 📱 Responsive Layout               │
│                                         │
│ 🧪 Testing Skills                       │
│   ├─ ✅ Unit Testing                    │
│   └─ 🔄 Integration Testing             │
└─────────────────────────────────────────┘
```

### 2.7 侧边栏 - Agent Library

**功能描述**：
- 右侧面板显示所有可用 Agents
- 显示每个 Agent 的专长和推荐场景
- 拖拽到画布加载 Agent

**UI 布局**：
```
┌─────────────────────────────────────────┐
│ Agent Library                           │
├─────────────────────────────────────────┤
│ 👨‍💻 Backend Developer                    │
│    专长: API, Database, Security        │
│    推荐: 后端服务开发                    │
│                                         │
│ 👩‍💻 Frontend Developer                   │
│    专长: React, UI/UX, Performance      │
│    推荐: 前端应用开发                    │
│                                         │
│ 🔧 DevOps Engineer                      │
│    专长: CI/CD, Docker, Kubernetes      │
│    推荐: 部署和运维                      │
│                                         │
│ 🛡️  Security Expert                     │
│    专长: Penetration Testing, Audit     │
│    推荐: 安全审计和加固                  │
└─────────────────────────────────────────┘
```

### 2.8 配置持久化

**功能描述**：
- 保存画布配置到项目目录
- 支持导入/导出配置
- 版本控制友好（JSON 格式）

**配置文件结构**：
```json
{
  "version": "1.0.0",
  "project": {
    "id": "project-1",
    "name": "My Project",
    "path": "/path/to/project"
  },
  "nodes": [
    {
      "id": "skill-1",
      "type": "skill",
      "skillId": "api-development",
      "enabled": true,
      "position": { "x": 100, "y": 200 }
    },
    {
      "id": "agent-1",
      "type": "agent",
      "role": "backend-dev",
      "enabled": true,
      "position": { "x": 300, "y": 200 }
    }
  ],
  "connections": [
    {
      "id": "conn-1",
      "source": "skill-1",
      "target": "project-1",
      "type": "skill-to-project",
      "enabled": true
    }
  ]
}
```

**保存位置**：
- `.onecompany/canvas-config.json`

### 2.9 实时预览和验证

**功能描述**：
- 显示当前配置的有效性
- 检测冲突和依赖问题
- 提供配置建议

**验证规则**：
- 检查 Skill 依赖是否满足
- 检查 Agent 和 Skill 的兼容性
- 警告未使用的节点
- 提示推荐的配置

## 3. 用户交互流程

### 3.1 初始化项目配置

```
1. 用户打开 Canvas 应用
2. 选择或创建项目
3. 项目节点自动出现在画布中心
4. 左右侧边栏显示可用 Skills 和 Agents
```

### 3.2 添加 Skill

```
1. 从左侧 Skills Catalog 拖拽 Skill
2. 放置到画布上
3. 自动创建 Skill 节点
4. 拖拽连接点到项目节点
5. 连接线创建，Skill 启用
```

### 3.3 添加 Agent

```
1. 从右侧 Agent Library 拖拽 Agent
2. 放置到画布上
3. 自动创建 Agent 节点
4. 可选：连接 Skills 到 Agent（增强专长）
5. 连接 Agent 到项目节点
6. Agent 加载到项目
```

### 3.4 配置和管理

```
1. 点击节点切换启用/禁用
2. 双击节点打开配置面板
3. 右键菜单进行操作
4. 自动保存配置
```

## 4. 技术架构

### 4.1 前端技术栈

- **框架**: React 18+ with TypeScript
- **Canvas 库**: React Flow (推荐) 或 Konva.js
- **状态管理**: Zustand 或 Jotai
- **UI 组件**: shadcn/ui + Tailwind CSS
- **拖拽**: react-dnd 或 React Flow 内置
- **图标**: Lucide React

### 4.2 后端集成

- 读取 `@onecompany/skills-catalog`
- 集成 `@onecompany/core` 的 Agent 系统
- 保存配置到 `.onecompany/canvas-config.json`
- 提供 API 接口（可选）

### 4.3 项目结构

```
packages/canvas-app/
├── src/
│   ├── components/
│   │   ├── Canvas/
│   │   │   ├── Canvas.tsx           # 主画布组件
│   │   │   ├── SkillNode.tsx        # Skill 节点
│   │   │   ├── AgentNode.tsx        # Agent 节点
│   │   │   ├── ProjectNode.tsx      # 项目节点
│   │   │   └── ConnectionEdge.tsx   # 连接线
│   │   ├── Sidebar/
│   │   │   ├── SkillsCatalog.tsx    # Skills 侧边栏
│   │   │   └── AgentLibrary.tsx     # Agents 侧边栏
│   │   └── ConfigPanel/
│   │       ├── NodeConfig.tsx       # 节点配置面板
│   │       └── Validation.tsx       # 验证提示
│   ├── hooks/
│   │   ├── useCanvas.ts             # Canvas 状态管理
│   │   ├── useNodes.ts              # 节点管理
│   │   └── useConnections.ts        # 连接管理
│   ├── store/
│   │   └── canvasStore.ts           # 全局状态
│   ├── types/
│   │   └── canvas.types.ts          # 类型定义
│   └── utils/
│       ├── validation.ts            # 配置验证
│       └── persistence.ts           # 持久化
└── package.json
```

## 5. 数据流

```
┌─────────────────────────────────────────────────────┐
│                   Canvas UI                         │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐     │
│  │  Skill   │───▶│ Project  │◀───│  Agent   │     │
│  │  Node    │    │  Node    │    │  Node    │     │
│  └──────────┘    └──────────┘    └──────────┘     │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
         ┌────────────────────────┐
         │   Canvas Store         │
         │  (Zustand/Jotai)       │
         └────────────┬───────────┘
                      │
          ┌───────────┴───────────┐
          ▼                       ▼
┌──────────────────┐    ┌──────────────────┐
│ Skills Catalog   │    │  Agent Manager   │
│ (@onecompany/    │    │  (@onecompany/   │
│  skills-catalog) │    │   core)          │
└──────────────────┘    └──────────────────┘
          │                       │
          └───────────┬───────────┘
                      ▼
         ┌────────────────────────┐
         │  .onecompany/          │
         │  canvas-config.json    │
         └────────────────────────┘
```

## 6. 实现步骤

### Phase 1: 基础画布 (Week 1)
- [ ] 设置 React + TypeScript 项目
- [ ] 集成 React Flow
- [ ] 实现基础画布（缩放、平移）
- [ ] 创建项目节点组件

### Phase 2: Skill 系统 (Week 2)
- [ ] 创建 Skill 节点组件
- [ ] 实现 Skills Catalog 侧边栏
- [ ] 实现拖拽功能
- [ ] 实现连接线系统
- [ ] 实现启用/禁用开关

### Phase 3: Agent 系统 (Week 3)
- [ ] 创建 Agent 节点组件
- [ ] 实现 Agent Library 侧边栏
- [ ] 实现 Agent 拖拽和连接
- [ ] 集成 @onecompany/core Agent 系统

### Phase 4: 配置管理 (Week 4)
- [ ] 实现配置持久化
- [ ] 实现导入/导出功能
- [ ] 实现配置验证
- [ ] 添加实时预览

### Phase 5: 优化和测试 (Week 5)
- [ ] 性能优化（虚拟化、懒加载）
- [ ] 添加动画效果
- [ ] 编写单元测试和 E2E 测试
- [ ] 用户体验优化

## 7. 验收标准

- [ ] 可以流畅拖拽 Skills 和 Agents 到画布
- [ ] 可以通过连接线关联节点
- [ ] 可以切换节点的启用/禁用状态
- [ ] 配置自动保存到 `.onecompany/canvas-config.json`
- [ ] 配置可以正确加载和恢复
- [ ] 验证系统能检测配置问题
- [ ] 响应式设计，支持不同屏幕尺寸
- [ ] 性能良好（100+ 节点不卡顿）

## 8. UI/UX 设计要点

### 8.1 视觉设计
- 使用现代扁平化设计
- 深色主题为主（可选浅色主题）
- 节点使用卡片样式，带阴影
- 连接线使用贝塞尔曲线，流畅美观
- 启用状态用绿色，禁用用灰色

### 8.2 交互设计
- 拖拽时显示半透明预览
- 连接时显示吸附效果
- 悬停显示详细信息
- 操作有即时反馈（动画、提示）
- 支持键盘快捷键（Delete、Ctrl+Z 等）

### 8.3 响应式设计
- 桌面端：侧边栏固定
- 平板端：侧边栏可折叠
- 移动端：底部抽屉式面板

## 9. 扩展功能（未来）

- [ ] 多人协作（实时同步）
- [ ] 模板系统（预设配置）
- [ ] AI 推荐（智能建议配置）
- [ ] 历史版本管理
- [ ] 配置对比和合并
- [ ] 导出为图片/PDF
- [ ] 集成到 VS Code 扩展

## 10. 参考设计

类似产品：
- Figma（拖拽和画布交互）
- Node-RED（流程编排）
- Retool（组件连接）
- n8n（工作流可视化）
